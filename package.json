{
  "name": "slackmail-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Slack-powered email client monorepo",
  "type": "module",
  "scripts": {
    "build": "tsc --build --clean && tsc -p packages/core/tsconfig.build.json && tsc -p infra/aws/tsconfig.build.json",
    "lint": "biome lint packages infra",
    "format": "biome format packages infra --write",
    "check": "biome check packages infra",
    "fix": "biome check packages infra --write",
    "test": "pnpm -r test",
    "prepare": "husky"
  },
  "keywords": [
    "slack",
    "email",
    "bot"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "@types/node": "25.0.3",
    "husky": "9.1.7",
    "lint-staged": "^16.2.7",
    "typescript": "5.9.3",
    "vite-tsconfig-paths": "6.0.3",
    "vitest": "4.0.16"
  },
  "engines": {
    "node": "24.12.0"
  }
}
